MeasurementTool:
  selectors:
    - '"Guidelines" in dataset.dataset_description'
    - interestects(dataset.dataset_description.Guidelines, ["phenotype"])
    - datatype == "phenotype"
    - path != "phenotype/demographics.tsv"
  fields:
    MeasurementToolMetadata: Recommended
