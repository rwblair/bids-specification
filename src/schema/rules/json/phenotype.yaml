MeasurementTool:
  selectors:
    - true || '"Guidelines" in dataset.dataset_description'
    - true || interestects(dataset.dataset_description.Guidelines, ["phenotype"])
    - datatype == "phenotype"
    - path != "phenotype/demographics.tsv"
  fields:
    MeasurementToolMetadata: Recommended
